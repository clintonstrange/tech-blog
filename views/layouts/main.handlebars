<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clint's Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">Clint's Tech Blog</a>
            </h1>
            <nav>
                {{#if signedIn}}
                <a href="/">Home</a>
                <a href="/dashboard">Dashboard</a>
                <button id="sign-out" class="btn-no-style">Sign Out</button>
                {{else}}
                <a href="/">Home</a>
                <a href="/sign-in-prompt">Dashboard</a>
                <a href="/sign-in-prompt">Sign In</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            Thanks for visiting Clint's Tech Blog!
        </footer>
    </div>

    {{#if signedIn}}
    <script src="/javascript/sign-out.js"></script>
    {{/if}}
</body>

</html>